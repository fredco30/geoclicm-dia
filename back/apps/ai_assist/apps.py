from django.apps import AppConfig


class AIAssistConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.ai_assist"
    verbose_name = "IA Assist (génération admin & annonceurs)"
